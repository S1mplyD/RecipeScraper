const { recipeModel }= require("../database/recipe.model")

const convertRecipeToText = async ()=>{
    const recipes = await recipeModel.find()
}

const convertGZ = async ()=>{}

const convertAllRecipe = async ()=>{}
